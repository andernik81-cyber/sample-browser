import { useCallback, useEffect, useMemo, useRef, useState } from 'react'

/* Existing application source is being relocated to frontend/src without UI redesign. */

export default function App() {
  return <div className="sample-browser-app">Sample Browser</div>
}
